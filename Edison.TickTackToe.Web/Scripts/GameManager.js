function GameManager(hub, invName, oppName, curUserName, gid) {
	var gamesHub = hub;
	var invitatorName = invName;
	var currentUserName = curUserName;
    var gameId = gid;

	this.makeStep = function(figureId) {
	    // hub does something +
		// + gui
	}
}